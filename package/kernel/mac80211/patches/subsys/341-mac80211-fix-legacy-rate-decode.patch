--- a/net/mac80211/sta_info.c
+++ b/net/mac80211/sta_info.c
@@ -2364,6 +2364,9 @@ static void sta_stats_decode_rate(struct
 
 		sband = local->hw.wiphy->bands[band];
 
+		if (WARN_ON_ONCE(!sband))
+			break;
+
 		if (WARN_ON_ONCE(!sband->bitrates))
 			break;
 
